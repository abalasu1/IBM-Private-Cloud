apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  labels:
    app: processor
  name: processor
  namespace: calculator
spec:
  lookupPolicy:
    local: false
status:
  dockerImageRepository: docker-registry.default.svc:5000/calculator/processor
  tags:
  - items:
    tag: latest

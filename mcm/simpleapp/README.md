# Simple Nginx chart with MCM artifacts

Install instructions:

- Set up kubectl and helm cli 

- kubectl apply -f simpleapp -n simpleapp

- Edit placement policy to move application from one cluster to another